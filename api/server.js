'use strict';

// Compatibility entry point. Keep one server implementation so Vercel, Fly,
// local development, and security review all execute the same code.
module.exports = require('../server');
